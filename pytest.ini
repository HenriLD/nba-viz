[pytest]
testpaths = tests
addopts = -q
# Put the repo root on sys.path so `import app/core/ingest` works under a bare
# `pytest` invocation (CI), not just `python -m pytest` (which adds the cwd).
pythonpath = .
